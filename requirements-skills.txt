# Declared skill-script dependencies.
#
# Install path (Phase 0 spike will confirm which Hermes supports):
#   - if Hermes auto-installs a skill's declared deps → nothing to do here
#   - else, once per Hermes container:  uv pip install -r requirements-skills.txt
#
# Brand knowledge is authored as YAML (PyYAML). httpx is for outbound HTTP (Growi results, and the
# live eval's OpenRouter calls). Everything else in the bundle is Python stdlib.
pyyaml>=6.0
httpx>=0.27
